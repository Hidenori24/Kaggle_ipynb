# Extra dependencies needed only to re-run notebooks/ locally (EDA plots,
# jupyter nbconvert execution). Not needed to run the agent, the test suite,
# or tools/evaluate.py -- see requirements.txt for that minimal set.
-r requirements.txt
jupyter
nbconvert
matplotlib
pandas
numpy
